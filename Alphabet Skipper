#include <stdio.h>
#include<conio.h>
int main() 
{
    char ch='a';
    do {
        printf("%c ",ch);
        if(ch=ch+4) {
            continue;
        }
        ch++;
    } while(ch<='z');
    getch();
}
